version: '3'
services:
  api:
    build: ./
    image: car-api
    container_name: api
    ports:
      - "3000:3000"
    depends_on:
      - mongo

  mongo:
    image: mongo
    container_name: mongodb
    ports:
      - "27017:27017"
